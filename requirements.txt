# Épinglé (durcissement) : un projet généré dans 6 mois doit builder avec les
# versions validées par la suite de tests — les montées de version sont des
# décisions volontaires, pas des accidents de `pip install`.
fastapi==0.139.0
uvicorn[standard]==0.51.0
# Production (Chap 21) : Gunicorn orchestre des workers Uvicorn. uvicorn-worker
# remplace uvicorn.workers, déprécié depuis uvicorn 0.30.
gunicorn==26.0.0
uvicorn-worker==0.4.0
pydantic-settings==2.14.2
sqlalchemy[asyncio]==2.0.51
alembic==1.18.5
asyncpg==0.31.0
aiosqlite==0.22.1
pwdlib[argon2]==0.3.0
pyjwt==2.13.0
email-validator==2.3.0
psutil==7.2.2
pytest==8.4.1
pytest-asyncio==1.1.0
httpx==0.28.1
# Arène (couche C1) : anthropic pour la boucle agent (tool-use natif,
# correspond aux contrats d'outils de l'Annexe C) ; numpy pour les
# indicateurs de marché (SMA/EMA/RSI, chapitre 14). Décision volontaire
# documentée dans le plan C1, pas un accident de `pip install`.
# 0.37.1 -> 0.40.0 (2026-09-07) : 0.37.1 construit son AsyncClient httpx
# interne avec `proxies=`, un paramètre que httpx==0.28.1 (ci-dessus) a
# supprimé — AnthropicLLMClient plantait dès l'instanciation dès qu'une
# vraie clé API était présente (ScriptedLLMClient ne passe jamais par ce
# chemin, d'où l'absence de détection avant un déploiement avec la clé
# réellement positionnée). 0.40.0 est la première version qui a retiré ce
# `proxies=` interne ; formes de réponse (Message/Usage/ToolUseBlock/
# TextBlock) vérifiées identiques à ce qu'attend agents/llm_client.py.
anthropic==0.40.0
numpy==2.2.1
